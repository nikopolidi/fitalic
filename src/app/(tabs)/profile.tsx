import React from 'react';
import { ProfileContainer } from '../../components/profile';

/**
 * Profile tab screen for user profile and anthropometry tracking
 */
export default function ProfileScreen() {
  return <ProfileContainer />;
}
